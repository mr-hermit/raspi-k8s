---
# Self-signed ClusterIssuer — suitable for a local lab.
# Annotate any Ingress with: cert-manager.io/cluster-issuer: selfsigned
apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: selfsigned
spec:
  selfSigned: {}
